# Stable-schema reviewer timing check — September 14, 2026

The user's “next please” continues the authorized Preview reviewer validation.
The prior v1 schema-error and v2 timeout pairs stay closed; their claims and
retained evidence remain intact. This registration measures the shared reviewer
with its corrected static schema and streaming transport diagnostics.

At most two Anthropic calls, one per fixed retained candidate; no Google calls,
new artwork, classifier, format repair or SDK retry. The complete original brief,
images, model, 3050-token cap, reference feature rubric and acceptance thresholds
are unchanged. `stream: true` is the only new provider request option. Streaming
is opt-in for this Preview calibration, not yet a customer-path rollout.

| Case | Dataset | Source |
| --- | --- | --- |
| mismatched | meekah-stream-comparison-20260914-v1-mismatched | Human-rejected scene293 |
| matched | meekah-stream-comparison-20260914-v1-matched | Confirmed portrait289 |

Input hashes, sizes, host brief and reference provenance are fixed in
IDENTITY_FEATURE_COMPARISON.md. Both requests differ only in candidate bytes;
human labels and expected outcomes never enter provider messages. Portrait289 is
only an identity control, not approved gouache artwork or a complete invitation.

Owner-private route: `/api/events/owner/:ownerToken/prepayment-preview/stream-comparison/:caseId`.
Only the literal confirmation and fixed asset/reference hashes are accepted.
Preview branch codex/launch-blockers, synthetic unchanged event61 only. Durable
global claims and readback precede dispatch. Each critic retains the45-second
deadline. Run mismatched first on a verified READY deployment and successful CI;
only an accounted complete first result permits matched on the same deployment.
A semantic miss may proceed to measure discrimination. Provider error, timeout,
partial response, unknown outcome or retention failure stops the pair; neither
claim may be retried or reopened. All results remain rejected/private with no
preview activation, customer event change, merge or Production deployment.

Private server timing records headers, first SDK event, first text, finish,
request/message IDs, character count, stop reason and cumulative token usage.
Incomplete streams never reach verdict parsing. Partial usage is labelled and
is not a final bill. These times cannot isolate provider queue versus compilation;
headers alone do not establish compilation completion. Only the full result can
be used to assess likeness. Neither a passing pair nor a warm schema measurement
establishes broad image consistency or the90-second customer launch gate.
