# Seven-case reviewer follow-up

The owner said “proceed” after the five-call pilot stopped and the next action was described as adjusting research sequencing so honestly uncertain artwork remains rejected while unrelated cases continue. This follow-up covers only c06–c12 from the frozen twelve-case manifest. The initial five calls remain consumed and their study remains closed. The prior eight-image/brief disclosure to Anthropic is unchanged; at most seven additional physical Sonnet 4.6 reviews keep the combined total at twelve and cumulative planning reserve at $1.50, including $0.162783 already incurred. No images, classifiers, repairs, retries, replacements, customer activation or Production changes.

No reviewer prompts, schemas, scores, pass thresholds, customer rejection rules or frozen case inputs change. `researchReviewDisposition` is used only to sequence private research. A valid report may describe good or defective artwork. A complete, located, sub-five uncertain assessment may be recorded as `unresolved`; its original `passed:false` and invalid certainty flag remain untouched. Missing/duplicate/unknown checklist items, absent evidence, bad criteria, contradictory scores, brief-compliance craft penalties, malformed output, unavailable providers, unknown accounting and request/pixel/retention mismatches still stop the study. Medium uncertainty cannot coexist with a passed medium requirement or perfect fidelity. Unresolved execution basis is allowed only with an explicitly uncertain sub-five execution assessment.

The seven requests retain their original order and byte fingerprints: Rumi requested as Zoey; original vector; same vector with watercolor requested; construction watercolor; KPop Candidate B; Scene A; known menu lettering. Expected labels remain outside all model input. Claim keys use `cross-theme-review-20260916-followup-v1` and only these never-dispatched cases. The predecessor's exact five accounted results and terminal closure are prerequisites. A terminal closure follows the seventh review or a fatal failure. Every stored result remains rejected/no preview, even if its reviewer passed.

Report uncertainty separately, preserve all outcomes and independent human labels, and do not treat this small retained-image study as generator or launch qualification. No re-run of c01–c05 is authorized by this protocol.

## Observed stop and offline next step

The follow-up ran c06–c08, then closed at c08 for `compositionQuality:clear-score-conflict`. It is not resumable. Same gallery pixels scored craft5/layout5 under the original medium and craft2/layout4 under requested watercolor. The reduced craft rationale explicitly cited missing watercolor despite claiming an observed-craft basis. The layout assessment was clear/none/balanced while its numeric score was4. These are not honest uncertainty; the revised protocol correctly stopped.

The offline-only `independentCraftReview` prototype therefore accepts image bytes alone—no requested identity, medium, host brief, prior scores or expected labels. It builds a deterministic fingerprint so one exact image can have one retained craft assessment across differing briefs. It evaluates observed execution/organization only. No provider function, route, customer integration, verdict merger, cache persistence or paid allowance is implemented or enabled. Evidence from this prototype is request isolation and synthetic response handling, not improved live scoring.

Before live adoption, the full-brief reviewer must have a separate, complete fidelity contract covering all subjects, versions, scene facts, medium, counts, exclusions, intended layout, age and purchase suitability. Both components must bind to the same pixels and both must pass. No component may overwrite a failed score or upgrade an old approval. Extra-call cost and parallel latency require a fresh bounded test. The original production reviewer, flags and thresholds remain unchanged.

## Completed separate contract

The subsequent owner instruction “complete please” completed the offline brief contract and combination. `briefFidelityReview` preserves all host brief fields and inspiration, every required item and exclusion, free-form medium, named identities/versions, explicit count evidence, intended final surface, age, lettering and purchase suitability. Preferences stay optional. Attached identity references, when supplied, require the existing four-feature comparisons. The static schema does not expand with the number of requirements. Located honest uncertainty is retained and cannot pass; malformed or contradictory reports fail integrity.

`separatedArtworkReview` rebuilds the expected requests from trusted input and binds each component receipt to exact candidate bytes, request fingerprint, schema and model. Both complete reports must independently pass. It rejects missing reports, changed briefs/images, incomplete responses, retry counts, unknown usage and malformed JSON. It retains both raw answers, never averages or replaces scores, and supports one craft receipt for the same image across different briefs. The image-only craft validator also rejects extra fields rather than silently ignoring them.

These are preparation, validation and combination functions. They have no provider dispatcher, customer route, persistent cache or customer activation. Existing production calls and all closed study routes are unchanged. The new tests verify contracts using synthetic responses, not the model's visual accuracy. A new live study must have its own explicit scope, cost reserve and durable per-call claims before dispatch; no prior allowance is reopened by this code.

Proposed live validation uses the same eight retained images and twelve original full briefs with the new schema: eight distinct image-only craft requests plus twelve fidelity requests, at most twenty single provider calls. Proposed fresh planning reserve: $2.50, separate from all consumed or closed allowances and not a provider-enforced dollar cap. The prepared packets reserve $1.713 in total when planning 15,000 input tokens per request plus each bounded output maximum, at Sonnet4.6 standard rates of $3/$15 per million input/output tokens; these input estimates are not measured usage. No new generation, classification, repair or retry. This proposed scope has not been approved or dispatched.

Reuse the one craft receipt per exact image for paired identity/medium briefs. No prior score or expected label enters a request. Freeze fingerprints first, retain every outcome, keep all images private, and close on malformed/contradictory response, changed input, unknown cost, provider or retention failure. Honest uncertainty remains a rejection and permits unrelated cases. Assess structural validity and independent visual agreement separately. Do not call this generator qualification or a launch pass.
