name: Apply Teaser Physical Quality

on:
  push:
    branches:
      - codex/launch-blockers
    paths:
      - .github/workflows/apply-teaser-physical-quality.yml

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    timeout-minutes: 25
    env:
      DATABASE_URL: postgres://test:test@127.0.0.1:5432/test
      ANTHROPIC_API_KEY: test
      OPENAI_API_KEY: test
      VERCEL_ENV: local
      VERCEL_GIT_COMMIT_REF: ci-validation
    steps:
      - uses: actions/checkout@v4
        with:
          ref: codex/launch-blockers

      # Re-run with every existing physical-quality guard preserved.
      - name: Apply physical-quality prompt repair
        run: python script/patch_teaser_physical_quality.py

      - name: Verify exact repair
        run: |
          set -euo pipefail
          git diff --check
          npm ci
          npm run check
          npm test
          npm run vercel-build

      - name: Commit verified repair and retire patch runner
        run: |
          set -euo pipefail
          git rm .github/workflows/apply-teaser-physical-quality.yml script/patch_teaser_physical_quality.py
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add server/prePaymentPreviewQuality.ts tests/prePaymentPreviewQuality.test.ts
          git diff --cached --check
          git commit -m 'Reduce physical artifacts in first-look candidates'
          git push origin HEAD:codex/launch-blockers
